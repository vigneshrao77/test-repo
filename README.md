# Simple user form

## Setup

1. Create a PostgreSQL database, for example `usersdb`.
2. Copy `.env.example` to `.env` and set `DATABASE_URL`.
3. Install dependencies and start the app:

```bash
npm install
npm start
```

Open <http://localhost:3000>. The `users` table is created automatically on startup.
